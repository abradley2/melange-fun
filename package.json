{
  "scripts": {
    "build": "esbuild _build/default/src/app/src/Main.js --bundle --minify --sourcemap --target=es2015 --outfile=public/main.dist.js"
  },
  "devDependencies": {
    "esbuild": "^0.18.8"
  }
}
